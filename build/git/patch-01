--- Makefile.orig	2009-12-24 15:41:56.000000000 +0900
+++ Makefile	2009-12-24 15:42:25.000000000 +0900
@@ -1112,7 +1112,7 @@
 	EXTLIBS += $(ICONV_LINK) -liconv
 endif
 ifdef NEEDS_LIBGEN
-	EXTLIBS += -lgen
+	#EXTLIBS += -lgen
 endif
 ifdef NEEDS_SOCKET
 	EXTLIBS += -lsocket
@@ -1121,7 +1121,7 @@
 	EXTLIBS += -lnsl
 endif
 ifdef NEEDS_RESOLV
-	EXTLIBS += -lresolv
+	#EXTLIBS += -lresolv
 endif
 ifdef NO_D_TYPE_IN_DIRENT
 	BASIC_CFLAGS += -DNO_D_TYPE_IN_DIRENT
