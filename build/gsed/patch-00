--- sed/utils.c.orig	Fri Nov 27 13:19:53 2009
+++ sed/utils.c	Fri Nov 27 13:20:54 2009
@@ -285,7 +285,7 @@
 {
   clearerr(stream);
   if (fflush(stream) == EOF && errno != EBADF)
-    panic("couldn't flush %s: %s", utils_fp_name(stream), strerror(errno));
+    /*panic("couldn't flush %s: %s", utils_fp_name(stream), strerror(errno))*/;
 }
 
 /* Panic on failing fclose */
