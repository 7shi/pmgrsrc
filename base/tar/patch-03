--- src/list.c.orig	2010-01-02 02:08:03.000000000 +0000
+++ src/list.c	2010-01-02 02:08:06.000000000 +0000
@@ -850,10 +850,12 @@
 	*--value_string = '-';
       if (minus_minval)
 	*--minval_string = '-';
+#ifndef _MINIX
       /* TRANSLATORS: Second %s is type name (gid_t,uid_t,etc.) */
       ERROR ((0, 0, _("Archive value %s is out of %s range %s..%s"),
 	      value_string, type,
 	      minval_string, STRINGIFY_BIGINT (maxval, maxval_buf)));
+#endif
     }
 
   return -1;
